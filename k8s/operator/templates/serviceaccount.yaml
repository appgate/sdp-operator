apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sdp-operator.serviceAccountName" .}}
  namespace: {{ .Release.Namespace }}
  labels:
  {{- include "sdp-operator.labels" . | nindent 4 }}
---
{{- if has "git-operator" .Values.sdp.operators }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sdp-operator.serviceAccountName" .}}-git-operator
  namespace: {{ .Release.Namespace }}
  labels:
  {{- include "sdp-operator.labels" . | nindent 4 }}
{{- end }}
